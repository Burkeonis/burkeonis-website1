(()=>{const KEY='burkeonis_misophonia_v1';const form=document.getElementById('miso-form');const state=document.getElementById('miso-state');const fields=[...form.querySelectorAll('textarea')];const setState=t=>state.textContent=t;const save=()=>{const data={};fields.forEach(f=>data[f.name]=f.value);localStorage.setItem(KEY,JSON.stringify(data));setState('SAVED IN THIS BROWSER');};try{const data=JSON.parse(localStorage.getItem(KEY)||'{}');fields.forEach(f=>f.value=data[f.name]||'');if(Object.values(data).some(Boolean))setState('RESTORED');}catch(e){}document.getElementById('miso-save').addEventListener('click',save);fields.forEach(f=>f.addEventListener('input',()=>setState('UNSAVED CHANGES')));document.getElementById('miso-clear').addEventListener('click',()=>{if(confirm('Erase all saved misophonia notes from this browser?')){fields.forEach(f=>f.value='');localStorage.removeItem(KEY);setState('ERASED');}});document.getElementById('miso-print').addEventListener('click',()=>window.print());document.getElementById('miso-export').addEventListener('click',()=>{save();let text='BURKEONIS — MISOPHONIA TRIGGER MAP\n\n';fields.forEach(f=>{text+=f.closest('label').childNodes[0].textContent.trim().toUpperCase()+'\n'+(f.value||'[No response]')+'\n\n';});const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type:'text/plain'}));a.download='burkeonis-misophonia-trigger-map.txt';a.click();URL.revokeObjectURL(a.href);});let remaining=90,interval=null;const display=document.getElementById('timer');const toggle=document.getElementById('timer-toggle');const render=()=>display.textContent=`${String(Math.floor(remaining/60)).padStart(2,'0')}:${String(remaining%60).padStart(2,'0')}`;toggle.addEventListener('click',()=>{if(interval){clearInterval(interval);interval=null;toggle.textContent='RESUME';return;}toggle.textContent='PAUSE';interval=setInterval(()=>{remaining--;render();if(remaining<=0){clearInterval(interval);interval=null;toggle.textContent='COMPLETE';}},1000);});document.getElementById('timer-reset').addEventListener('click',()=>{clearInterval(interval);interval=null;remaining=90;render();toggle.textContent='START 90 SECONDS';});document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(b.dataset.copy);document.getElementById('copy-state').textContent='BOUNDARY COPIED';}catch(e){document.getElementById('copy-state').textContent=b.dataset.copy;}}));render();})();